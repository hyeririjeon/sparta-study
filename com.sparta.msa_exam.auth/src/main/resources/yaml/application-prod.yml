spring:
  datasource:
    url: jdbc:mysql://<RDS_ENDPOINT>:3306/shop
    username: <RDS_USERNAME>
    password: <RDS_PASSWORD>
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: false
        format_sql: false
        use_sql_comments: false